# Local Supabase only. Copy to .env.local and replace the placeholder with the
# browser-safe publishable key printed by `supabase status -o env`.
VITE_SUPABASE_URL=http://127.0.0.1:54321
VITE_SUPABASE_PUBLISHABLE_KEY=replace-with-local-publishable-key
