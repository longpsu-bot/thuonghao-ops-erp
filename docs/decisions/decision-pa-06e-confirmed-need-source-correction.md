# Decision PA-06E — Confirmed Need Source Correction

**Status:** Historical decision set with accepted PLANNING-CONTRACT-02B amendment; only rows explicitly labeled approved direction or amended below are settled

**Contract:** [PA-06E Confirmed Need Review, Adjustment, Revision, and Source-Correction Contract](../architecture/pa-06e-confirmed-need-review-adjustment-revision-contract.md)

**Implementation boundary:** [TASK-PA-06E Confirmed Need Revision Contract](../implementation-tasks/TASK-PA-06E-confirmed-need-revision-contract.md)

## 1. Context

The approved business contract already defines a Planning-owned `ConfirmedNeedBatch`, stable `ConfirmedNeedLine`, versioned `ConfirmedNeedLineRevision`, logical `ConfirmedNeedSourceReference`, logical `ConfirmedNeedAdjustment`, validation, approval snapshots, and separate approval/release states. Those business semantics are reusable.

The merged PA-04 physical implementation is narrower: its Confirmed Need batch, stable line, and line revision require wholesale order, wholesale line, and wholesale line-revision foreign keys. It has no physical school-catering source reference or complete line-level adjustment/decision evidence. Its revision payload is historically stable, while controlled `is_current` and `revision_status` metadata can change on supersession. Approval snapshots and downstream documents remain fully immutable.

PA-06D identified a future Planning review point but did not approve a new Quantity Decision object. PA-06E resolves that architectural question at the business-object level: the existing Confirmed Need aggregate remains the decision boundary, with append-only line decision evidence. H0 must generalize source lineage and decision-evidence persistence before school-catering H1. This decision does not modify `PA-05D.v1`; direct-wholesale remains exact pass-through, with theoretical, confirmed, approved, released, and handoff quantities equal.

The table below distinguishes directions already approved by the PA-06E brief or existing approved contracts from recommendations that still require product-owner or architecture approval. A proposed row is not an accepted project decision.

## 2. Decision records

| ID       | Decision                                             | Current behavior or approved fact                                                                                                                                        | Alternatives considered                                                                                                                            | Recommendation                                                                                                                                                                                                                           | Rationale                                                                                                   | Compatibility impact                                                                                   | Overengineering risk                                                                                                         | Product-owner status                                                             |
| -------- | ---------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------ | ---------------------------------------------------------------------------------------------------------------------------- | -------------------------------------------------------------------------------- |
| PA06E-01 | Business object                                      | The approved contract defines the Confirmed Need aggregate and logical source-reference/adjustment children; PA-04 implements only a wholesale-specific physical subset. | Add `PlanningQuantityConfirmation`; add a generic Quantity Decision; add an intermediate cross-domain workflow object; retain Confirmed Need.      | Retain the logical Confirmed Need aggregate and add no new aggregate; separately generalize its physical source/evidence representation for school catering.                                                                             | It is the smallest business model while accurately separating semantic reuse from schema readiness.         | Compatible with PA-02 and PA-05D; H0 migration design is required before school-catering H1.           | A second aggregate would duplicate lifecycle and reconciliation work.                                                        | **Aggregate direction approved; physical H0 design pending.**                    |
| PA06E-02 | Theoretical versus confirmed authority               | Existing line revisions persist both quantities; direct-wholesale currently requires equality.                                                                           | Collapse the values; treat any non-null confirmed value as an operator decision; use lifecycle-qualified meanings.                                 | Keep separate meanings. `theoretical_quantity` is calculated evidence; `confirmed_quantity` becomes decision-authoritative only after explicit Planning confirmation. A system proposal must be labeled as proposed in authorized reads. | Separates calculation from human accountability without changing the physical model.                        | Preserves direct-wholesale equality and the existing columns.                                          | Adding another quantity column merely to express UI proposal state is unnecessary until implementation evidence requires it. | **Approved direction in PA-06E brief; exact proposal persistence is pending.**   |
| PA06E-03 | Source calculation binding                           | The business contract requires `ConfirmedNeedSourceReference`; current PA-04 lineages only through required wholesale FKs.                                               | Resolve sources dynamically; fake wholesale rows; free-text/JSON polymorphism; dedicated or typed relational source evidence.                      | Bind every reviewed revision and snapshot to exact governed Need Generation/source evidence through an H0 typed relational model.                                                                                                        | Reproduction and correction require exact lineage, not a latest-source lookup or invented wholesale source. | Requires H0 persistence; preserves existing wholesale columns/behavior through explicit compatibility. | Copying full source payloads or adding generic polymorphism would be unsafe and unnecessary.                                 | **Exact lineage required; physical alternative pending.**                        |
| PA06E-04 | When to create a line revision                       | The model supports predecessor-linked current and superseded revisions; current tables lack complete line decision evidence.                                             | Revision per keystroke; overwrite Draft; revision only on approval; local Draft plus backend preview and revision/evidence at material boundaries. | Keep keystrokes local. Create a successor for a material quantity/item/unit/source/rule payload change. For unchanged acceptance, retain the Draft revision and append explicit decision evidence bound to its exact versions.           | Preserves material history and explicit accountability without unusable or redundant revisions.             | Requires H0 decision-evidence persistence and H1 confirmation contract.                                | Both per-keystroke revisions and identical no-change successors are excessive.                                               | **Direction approved; exact physical evidence shape pending.**                   |
| PA06E-05 | Batch approval strategy                              | Confirmed Need has batch approval snapshots. No evidence supports partial release.                                                                                       | Approve each line independently; approve selected lines; require all current lines reviewed and approve one complete batch snapshot.               | Use line-level review followed by one complete batch approval snapshot. Any stale, invalid, or unreviewed line blocks approval.                                                                                                          | Operations and downstream handoff need one coherent, reproducible commitment.                               | Uses existing batch and snapshot structures.                                                           | Partial-batch status and downstream reconciliation would add a second workflow without evidence.                             | **Pending product-owner approval; full-batch recommendation.**                   |
| PA06E-06 | Validation, approval, and release                    | The approved lifecycle and commands separately define `VALIDATED`, `APPROVED`, and `RELEASED_FOR_PURCHASE_HANDOFF`; PA-05D collapses them only for direct wholesale.     | Merge transitions; omit persisted validation; keep separate validation/approval/release commands.                                                  | Retain `validate_confirmed_needs`, then approve the exact validated version, then separately release its approval snapshot.                                                                                                              | Reconciles the parent contract and preserves complete-batch control and separation of duties.               | Direct wholesale remains a bounded atomic shortcut; school-catering functions are later work.          | Reusing the existing three transitions is simpler than inventing new statuses.                                               | **Parent lifecycle approved; API/security details pending.**                     |
| PA06E-07 | Correction before approval                           | Source changes must not rewrite history; Draft review is mutable only through controlled revisions.                                                                      | Patch the current line; silently recalculate; create a new calculation and successor line revision.                                                | Recalculate from the corrected source, create a new current Draft revision, supersede the prior Draft revision, and require review.                                                                                                      | The reviewer must see exactly what changed and why.                                                         | No effect on approved or released snapshots.                                                           | A generic correction case object is unnecessary.                                                                             | **Approved no-overwrite direction; exact command mechanics pending.**            |
| PA06E-08 | Correction after approval and before release         | An approval snapshot is immutable, and the batch lifecycle includes `REOPENED`.                                                                                          | Patch the approved revision; auto-revoke approval; explicit reopen and new revision.                                                               | Require an authorized explicit reopen with reason, preserve the old approval snapshot, create the corrected calculation and successor Draft revision, then review and approve a new snapshot.                                            | Approval evidence must remain explainable while corrected work becomes explicit.                            | Reuses the existing reopen state; downstream handoff does not yet exist.                               | A separate approval-revocation aggregate is unnecessary.                                                                     | **Approved immutability; reopen workflow recommendation pending.**               |
| PA06E-09 | Correction after release and before Purchase Handoff | A released snapshot is immutable. CMD-03 must consume an exact eligible release.                                                                                         | Mutate the release; let CMD-03 use latest source; reopen and block handoff until a new release; allow stale handoff.                               | Require explicit reopen and a new reviewed/approved/released snapshot. CMD-03 must fail closed if the eligible release is stale, superseded, or source-incompatible.                                                                     | Prevents a source correction from silently crossing the Planning/Procurement boundary.                      | `PA-05D.v1` remains unchanged; a school-catering CMD-03 contract change requires a separate task.      | Automatic cross-domain invalidation before a handoff exists is unnecessary.                                                  | **Approved no-overwrite direction; blocking/reopen mechanics pending.**          |
| PA06E-10 | Correction after Purchase Handoff, PO, or Dispatch   | Released downstream documents are immutable snapshots owned by their domains.                                                                                            | Cascade-update downstream facts; delete and recreate; domain-owned revision/compensation; generic cross-domain correction object.                  | Preserve every issued fact. A future separately approved contract must define Handoff supersession or replacement and any Procurement/Dispatch compensation. PA-06E emits no executable cross-domain behavior.                           | Domain ownership and legal/operational evidence are preserved.                                              | No current API, lifecycle, registry, or downstream document changes.                                   | Designing one universal correction workflow now would overfit unknown PO and Dispatch states.                                | **Immutability approved; correction variants pending.**                          |
| PA06E-11 | Recipe/BOM and other source corrections              | Recipes, BOMs, menus, attendance, units, mappings, and activation are upstream governed sources.                                                                         | Edit the Confirmed Need line directly; silently rerun; version the source, recalculate, and revise Confirmed Need.                                 | Correct/version the owning source, produce a new calculation, and create a pending-review successor Confirmed Need revision. Never rewrite prior calculation, approval, or release evidence.                                             | Maintains responsibility at the source while making operational consequences visible to Planning.           | Compatible with recipe governance and stable line identity when business identity remains the same.    | Copying upstream governance into Planning would blur module boundaries.                                                      | **Approved direction in PA-06E brief.**                                          |
| PA06E-12 | Prior confirmed quantity as a proposal               | No approved rule authorizes automatic carry-forward.                                                                                                                     | Never show it; auto-copy it as confirmed; show it only as a non-authoritative proposal when identity and policy remain compatible.                 | Permit a clearly labeled proposal only for the same stable line, ingredient, unit, service scope, and effective policy. Never auto-confirm it; do not carry it when ingredient or scope identity changes.                                | Useful operator context is retained without converting history into current authority.                      | Read/preview behavior only until a later command is approved.                                          | A machine-learning recommendation or generic preference engine is unnecessary.                                               | **Pending product-owner approval.**                                              |
| PA06E-13 | Reason requirement                                   | Existing contracts require adjustment/decision traceability, but the exact taxonomy is not settled.                                                                      | Always require free text; require only a code; require evidence only when values differ; code plus conditional note.                               | Require a governed reason when confirmed differs materially from theoretical or a prior confirmed value. Require a note for an `OTHER` reason and for policy-defined high-impact changes. Preserve actor and timestamp.                  | Creates useful audit evidence without demanding a note for every unchanged line.                            | Requires future policy and request-schema definition.                                                  | A broad case-management taxonomy would be excessive.                                                                         | **Direction approved; taxonomy and note thresholds pending.**                    |
| PA06E-14 | Exact zero quantity                                  | Physical constraints allow nonnegative quantities, but operational release behavior for zero is not approved.                                                            | Delete the line; auto-drop it; approve/release exact zero with reason; block all zero lines.                                                       | Preserve the stable line and revision. Allow zero as a reviewable proposal; require an explicit reason and product-approved release rule before it can enter a released batch. Never silently delete it.                                 | Zero can be a valid outcome or a data-quality signal, and both need traceability.                           | No change until the later policy/command task.                                                         | A special cancellation aggregate is unnecessary.                                                                             | **Pending product-owner approval.**                                              |
| PA06E-15 | Planning policy versioning                           | PA-06D distinguishes calculation scale from operational precision. The task fixture is `0.01 kg` Planning and advisory `0.1 kg` purchase.                                | Hard-code steps; use numeric column scale; version effective policy and fail closed.                                                               | Resolve one versioned effective Planning policy for preview/commit, bind it to evidence, and fail closed when missing, ambiguous, invalid, or changed. Fixture values are test data, not production defaults.                            | Reproducibility requires policy identity; numeric scale does not define business precision.                 | Compatible with PA-06D; production values and ownership remain unapproved.                             | A generic formula engine is explicitly unnecessary.                                                                          | **Versioned/fail-closed direction approved; values and owner pending.**          |
| PA06E-16 | Advisory purchase consequence                        | Procurement owns purchasable quantity; Planning owns confirmed operational need.                                                                                         | Persist purchase quantity in Planning; block confirmation on advisory; show a non-authoritative consequence only.                                  | A Planning preview may show an advisory purchase consequence derived from a supplied purchase-policy fixture, clearly labeled non-authoritative. It is not a Procurement fact and cannot be released as one.                             | Helps operators understand impact while preserving module ownership.                                        | No Purchase Handoff, allocation, PO, or Procurement writes.                                            | Duplicating the Procurement quantization engine in Planning would be excessive.                                              | **Boundary approved; advisory availability/blocking behavior pending.**          |
| PA06E-17 | API surface and first slice                          | The canonical PA-06A registry has no school-catering Confirmed Need materialization/review commands; current schema cannot support them.                                 | One generic mutation; many field-level commands; H0 materialization plus a bounded seven-function review/lifecycle family.                         | Track `create_confirmed_needs_from_generation` as the H0 API gap; propose seven review/lifecycle functions including validation. H1 is only read, preview, and confirm after H0. Do not modify the canonical registry.                   | Preserves the parent lifecycle, WYSIWYG, idempotency, stale checks, receipts, audit, and readback.          | All names/capabilities and H0/H1 implementations require separate approval.                            | A generic workflow or command framework is unnecessary.                                                                      | **Pending architecture and security approval.**                                  |
| PA06E-18 | Vietnamese terminology                               | Existing lifecycle terms remain usable; final labels for calculated/theoretical, proposed confirmed, and confirmed/released need are not approved.                       | `Nhu cầu tính toán`; `Nhu cầu lý thuyết`; `Nhu cầu đề xuất`; `Nhu cầu xác nhận`; `Nhu cầu vận hành`; `Nhu cầu đã phát hành`.                       | Keep technical English meanings normative. Present candidate Vietnamese labels in UI research and approve them with operations users before implementation.                                                                              | Avoids encoding an ambiguous operational term into schema, API, or training material.                       | No current UI or API label changes.                                                                    | A terminology subsystem is unnecessary.                                                                                      | **Pending product-owner and operations-language approval.**                      |
| PA06E-19 | Aggregate reuse versus schema readiness              | Logical Confirmed Need semantics are reusable; merged physical batches/lines/revisions require wholesale source FKs.                                                     | Claim current readiness; fake wholesale lineage; new aggregate; generalize the retained aggregate's physical source model.                         | State that business-object reuse is approved while school-catering physical readiness requires H0.                                                                                                                                       | Prevents semantic reuse from being mistaken for executable schema support.                                  | No PA-05D change; H0 must preserve its exact pass-through behavior.                                    | Hiding the mismatch would create fake data; a second aggregate would duplicate the domain.                                   | **Approved correction based on merged schema evidence.**                         |
| PA06E-20 | Source-reference persistence prerequisite            | Logical `ConfirmedNeedSourceReference` exists; no merged physical child represents school-catering generation/source lineage.                                            | Dedicated child; typed relations; bounded typed child family; free-text IDs; JSON; fake wholesale.                                                 | Prefer a bounded typed child family implementing the logical source reference with explicit Need Generation identity and declared typed source relations. Exact DDL remains pending.                                                     | Matches PA-02 typed-bridge rules and supports reproducible correction.                                      | Requires a separate migration/security/test contract before H1.                                        | Generic polymorphism is riskier than a few governed relations.                                                               | **Prerequisite approved; physical alternative pending H0 review.**               |
| PA06E-21 | Line decision-evidence persistence                   | Logical `ConfirmedNeedAdjustment` exists; PA-04 has only revision creator/command and batch approval evidence.                                                           | Append-only child; revision metadata; hybrid.                                                                                                      | Recommend hybrid: revision holds current quantity/source/rule payload; append-only child evidence holds actor/time, reason, before/after, exact versions, command, and acceptance kind.                                                  | Separates authoritative value from the evidence explaining human authority.                                 | H0 persistence and H1 command/read shapes must be approved together.                                   | A second aggregate or generic audit reconstruction is unnecessary.                                                           | **Evidence requirement approved; exact physical shape pending.**                 |
| PA06E-22 | Unchanged-proposal acceptance evidence               | A materialized Draft may already contain the proposed value; neither creator/command nor later approval proves line-level acceptance.                                    | Create identical successor; infer from approval; append explicit acceptance evidence to the same revision.                                         | Keep the exact Draft quantity revision and append `UNCHANGED_PROPOSAL_ACCEPTED` decision evidence bound to actor/time, batch, line, rule/source versions, and command; increment batch version once.                                     | Makes human authority explicit without manufacturing duplicate quantity history.                            | Requires the H0 child/evidence shape and H1 confirmation semantics.                                    | Identical successor revisions create noise without new payload.                                                              | **Explicit evidence required; naming/reason policy pending.**                    |
| PA06E-23 | Retained validation command                          | Parent contract defines `VALIDATED`, `ValidateConfirmedNeeds`, `ConfirmedNeedsValidated`, and `ConfirmedNeedValidationFailed`.                                           | Omit validation and amend parent; merge with approval; retain separate validation.                                                                 | Retain proposed `validate_confirmed_needs`; validate the full exact batch under locks, persist/return governed issue outcomes, and require its exact successful version for approval.                                                    | Directly reconciles the approved lifecycle and command/event contract.                                      | Added to the proposed seven-function family but excluded from H1.                                      | Removing a parent lifecycle state would require a larger amendment.                                                          | **Parent-contract behavior approved; implementation details pending.**           |
| PA06E-24 | Immutable payload versus lifecycle metadata          | Current revisions store mutable `is_current` and `revision_status`; snapshots and downstream rows reference exact revision IDs.                                          | Freeze entire row and add current pointer; mutate payload; allow only controlled metadata transition.                                              | Keep quantities/item/unit/source/predecessor/creation/command payload immutable; allow atomic current/status supersession metadata; never alter snapshots, releases, receipts, events, audit, or downstream references.                  | Is precise and compatible with the current schema while preserving traceability.                            | H0 may later choose a separate pointer only through an explicit migration decision.                    | Claiming whole-row immutability while updating lifecycle fields is internally inconsistent.                                  | **Approved invariant correction; alternate pointer design pending if proposed.** |
| PA06E-25 | Need Generation materialization                      | Parent contract defines `CreateConfirmedNeedsFromGeneration`; PA-05D only materializes direct-wholesale pass-through.                                                    | Source owner writes Confirmed Need; UI copies calculation; controlled Planning/integration materialization.                                        | H0 must define `create_confirmed_needs_from_generation`: consume an exact released generation result, create/refresh Draft lines and governed source references, set theoretical/proposed values, and never confirm Planning authority.  | Restores the missing calculation-to-review boundary and domain ownership.                                   | Required for production; an H1 fixture may provision a starting line only after generalized schema.    | Letting source modules or the browser author Planning facts would violate boundaries.                                        | **Parent responsibility approved; API and implementation pending H0.**           |
| PA06E-26 | H0 versus H1 order                                   | Current wholesale-only schema blocks a genuine school-catering read/preview/confirm slice.                                                                               | Implement H1 with fake lineage; combine everything in one broad task; H0 prerequisite then bounded H1.                                             | PA-06E-H0 contracts/implements source generalization, decision evidence, and materialization prerequisites; PA-06E-H1 later proves one-line authorized read/preview/confirm; validation/approval/release/CMD-03 remain later.            | Produces reviewable security and migration boundaries without hiding prerequisites.                         | Neither task is authorized by PA-06E; each needs separate acceptance and tests.                        | One broad slice would mix schema, security, commands, and workflow risk.                                                     | **Sequencing direction required; implementation not approved.**                  |

## 3. Accepted architectural directions

The following are settled for future design unless superseded through change control:

1. Use the existing logical Confirmed Need aggregate and revision model; add no Planning Quantity Confirmation or generic Quantity Decision aggregate. Do not claim the wholesale-only physical implementation is school-catering ready.
2. Keep calculated/theoretical quantity distinct from Planning-confirmed quantity.
3. Bind each decision to exact governed source/calculation, effective-policy, line-revision, actor/time, reason, before/after, batch version, and command evidence.
4. Correct upstream sources in their owning domains, let Need Generation calculate, and use the Planning materialization boundary to express the operational consequence as a Draft Confirmed Need revision.
5. Keep revision payload immutable while permitting controlled current/status supersession metadata; preserve approval snapshots, release evidence, handoff, PO, and Dispatch facts fully.
6. Keep direct-wholesale `PA-05D.v1` pass-through unchanged.
7. Keep Procurement authority over purchasable quantity and allocation; Planning may show only a labeled advisory consequence.
8. Retain separate complete-batch validation before approval and separate release afterward.
9. Require H0 source/decision persistence and materialization before H1 read/preview/confirm.
10. Treat the proposed API registry delta as non-canonical until a separately approved implementation task changes the registry.

### PLANNING-CONTRACT-02B accepted amendment

PLANNING-CONTRACT-02B supersedes the blanket-review implication in PA06E-07 and PA06E-11 and resolves PA06E-12 for correction-eligible `NEED_GENERATION` batches. An upstream source successor still makes the overall generated Need `OUTDATED`, but it does not automatically invalidate every human line decision.

The backend compares only the direct predecessor and successor current business facts inside the same Confirmed Need batch. Stable identity is exactly `service_date + customer_id + school_id + delivery_location_id + ingredient_id + controlled_unit_id`. A valid current human decision is carried only when that identity, the exact PostgreSQL `numeric(20,6)` theoretical quantity, and the sole effective Planning quantity-policy revision are unchanged. Dish, Recipe, Recipe version/line/revision, source membership, source signatures, Need Generation identities, release snapshots, and fingerprints remain lineage evidence and are not carry predicates.

Carry creates no human decision. The original decision ID, actor, time, confirmed quantity, reason, note, and command evidence remain authoritative through immutable `CARRIED_FORWARD` system evidence connecting the direct predecessor and successor revisions. Proposal change, policy incompatibility, and removal instead create exact immutable invalidation evidence and narrowly authorize pointer clearing. New or already-unreviewed lines receive no fake continuity evidence. A later return to an older quantity cannot resurrect an invalidated decision; reconfirmation appends the next human decision to the historical predecessor chain.

This amendment applies only before downstream release while the existing materialization lifecycle permits correction. `RELEASED_FOR_PURCHASE_HANDOFF` plus stale upstream evidence remains `DOWNSTREAM_CORRECTION_REQUIRED`; release snapshots and downstream facts are not mutated. PLANNING-CONTRACT-02A Recipe-replacement lineage semantics remain unchanged.

## 4. Explicit non-decisions

PA-06E does not approve:

- a SQL schema, migration, RPC, grant, RLS policy, event implementation, or generated type;
- a physical H0 source-reference/decision-evidence alternative, migration, materialization implementation, or H1 implementation;
- a React, Storybook, Retool, or other UI implementation;
- a new status, business aggregate, cross-domain correction workflow, or editable Purchase Handoff;
- school-catering CMD-03 behavior or any change to direct-wholesale CMD-03;
- production Planning or purchase step values;
- partial release, unsupported decision resurrection, source-change cascading, or downstream document rewriting;
- direct access from browser roles to private Planning tables;
- fake wholesale lineage, polymorphic free-text source IDs, caller-authored table names, or generic unvalidated JSON lineage;
- a live Supabase, OPS v1, production-data, package, credential, or deployment change.

## 5. Consequences

The corrected sequence remains narrow but explicit. H0 must first contract and later implement the school-catering persistence/source generalization, line decision evidence, and Need Generation materialization boundary. Only then may H1 implement an authorized review read, deterministic preview, and preview-bound confirmation for one synthetic line. Validation, approval, release, CMD-03 integration, Procurement, downstream correction, direct-wholesale changes, and live operations remain outside H1.

Product and architecture review must close the pending rows before their behavior can become authoritative. Approved rows may be referenced as requirements, but no proposed function or capability is part of the canonical API registry merely because it appears in PA-06E.

The accepted PLANNING-CONTRACT-02B amendment is authoritative for selective confirmation continuity and is implemented through private relational evidence and existing command surfaces; it does not accept any other pending PA-06E proposal.

## 6. Security, migration, and rollback effect

The historical PA-06E record was documentation-only. PLANNING-CONTRACT-02B implements its accepted amendment through a new private immutable continuity relation, backend-only helper/grant changes, amended existing command/read behavior, and bounded Application presentation. It adds no public API, operator capability, hosted mutation, live-system change, or deployment.

Future implementation must use backend authorization, exact scope, private tables, hardened function boundaries, idempotency, optimistic concurrency, preview/commit fidelity, safe errors, receipts, events, audit, and authoritative readback. Documentation rollback is a normal Git revert; no database rollback applies.
